# Substack Fixtures

Sanitized RSS fixtures for parser regression tests.

Coverage:
- single-item and multi-item RSS structures
- fallback fields (`description`, `content:encoded`)
- HTML stripping + excerpt truncation behavior
